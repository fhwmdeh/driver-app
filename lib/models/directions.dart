class Directions {
  String ? humarReadableAddress;
  String ? locationName;
  String ? locationId;
  double ? locationlatitude;
  double ? locationlongitude;

  Directions({
    this.humarReadableAddress,
    this.locationId,
    this.locationlatitude,
    this.locationlongitude,
    this.locationName,
});
}